flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/MAX_my_bill_icon.png"
  remove_alpha_ios: true
  min_sdk_android: 21

  # Adaptive icon configuration for Android (API 26+)
  # Using the icon image itself for both foreground and background
  # This ensures consistent appearance without white padding
  adaptive_icon_foreground: "assets/MAX_my_bill_icon.png"
